name: country_picker_flutter_plus_example
description: Example app for the country_picker_flutter_plus package.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  country_picker_flutter_plus:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
